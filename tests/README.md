# Cross-application Tests

This directory will contain verification that spans application or package boundaries.

Most tests should remain beside their owning code. This directory is reserved for protocol compatibility tests, packaged Desktop-to-Host smoke tests, shared fixtures, and other system behavior that cannot be owned meaningfully by one workspace.
