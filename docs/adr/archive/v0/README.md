# v0 Architecture Decision Archive

ADRs `0001`–`0024` record decisions made for the previous Space Zero implementation preserved on the `v0` branch.

These records are retained as historical evidence and implementation research. They are not normative for the active `v0.1` rebuild. Active decisions live in [`docs/adr/`](../../).
