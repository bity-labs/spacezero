# Scripts

This directory will contain small repository and release automation scripts.

Likely responsibilities include validation wrappers, packaged application smoke tests, artifact and Electron-fuse verification, Workspace Host health checks, and version consistency checks. Scripts should remain deterministic, documented, and suitable for local and CI execution.
