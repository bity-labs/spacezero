# Applications

This directory contains independently runnable or deployable Space Zero applications.

Applications may depend on shared packages, but shared packages must not depend on applications. New applications are added only when a concrete delivery slice requires a separate runtime or deployment target.
