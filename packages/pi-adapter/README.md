# Pi Adapter

Metadata/config/test setup only in the initialization slice.

The package has strict ESM metadata, exact Node/Effect pins, an explicit empty exports policy, and a package-policy test proving there is no implementation source and no Pi SDK dependency yet.

Deferred: Pi SDK dependency, authentication storage, conversations, resources, event translation, cancellation, cleanup, and Host-facing Effect services.
