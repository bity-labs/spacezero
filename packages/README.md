# Shared Packages

This directory contains libraries shared by two or more Space Zero runtimes or representing an explicit architecture boundary.

Packages must have narrow ownership and documented dependency direction. A new package should be created only when a current vertical slice needs the boundary; this directory is not a catalog of speculative future subsystems.
