# Host Contracts

This package will define the stable, versioned protocol shared by Workspace Host clients and the host.

It will contain commands, queries, events, projections, structured errors, protocol compatibility metadata, and runtime validation schemas. It must remain independent of Pi, Electron, React, Node filesystem services, and persistence implementations.
