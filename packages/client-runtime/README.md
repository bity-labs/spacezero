# Client Runtime

Active browser-safe package for the initialization slice.

It currently provides an empty public ESM entrypoint with package-import smoke coverage only. Effect HttpApi Client, fetch/SSE transport, capabilities, reconnect, cursor catch-up, projections, readiness state, and product commands/queries are deferred.
